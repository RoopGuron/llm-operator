apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
    control-plane: controller-manager
  name: llm-icp-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: llmoptimizedservices.ai.mlo.platform
spec:
  group: ai.mlo.platform
  names:
    kind: LLMOptimizedService
    listKind: LLMOptimizedServiceList
    plural: llmoptimizedservices
    singular: llmoptimizedservice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LLMOptimizedService is the Schema for the llmoptimizedservices
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LLMOptimizedService
            properties:
              idleTimeoutSeconds:
                description: |-
                  IdleTimeoutSeconds sets how long the queue-length metric must stay
                  below threshold before KEDA scales to 0. Only used when ScaleToZero is
                  true; defaults to 300 (5 minutes) if unset.
                format: int32
                type: integer
              maxConcurrencyPerPod:
                description: MaxConcurrencyPerPod sets parallel thread processing
                  density per engine
                format: int32
                type: integer
              maxReplicas:
                description: MaxReplicas sets the ceiling target for Week 3 KEDA structures
                format: int32
                type: integer
              minReplicas:
                description: MinReplicas sets the baseline scaling target floor
                format: int32
                type: integer
              modelPath:
                description: ModelPath represents the model layout name, e.g., "llama3"
                  or "mistral"
                type: string
              scaleToZero:
                description: |-
                  ScaleToZero lets KEDA scale the Deployment down to 0 replicas after
                  IdleTimeoutSeconds of no load, instead of holding at MinReplicas. Model
                  weights stay cached on the node's hostPath volume, so scaling back up
                  re-pulls quickly, but there is no automatic wake-on-request: at 0
                  replicas nothing is running to observe new traffic, so bringing
                  replicas back above 0 is a manual step (e.g. kubectl scale).
                type: boolean
            required:
            - maxConcurrencyPerPod
            - modelPath
            type: object
          status:
            description: status defines the observed state of LLMOptimizedService
            properties:
              availableReplicas:
                description: |-
                  conditions represent the current state of the LLMOptimizedService resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                format: int32
                type: integer
              phase:
                type: string
            required:
            - availableReplicas
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-controller-manager
  namespace: llm-icp-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-leader-election-role
  namespace: llm-icp-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-llmoptimizedservice-admin-role
rules:
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices
  verbs:
  - '*'
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-llmoptimizedservice-editor-role
rules:
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-llmoptimizedservice-viewer-role
rules:
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: llm-icp-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - services
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ai.mlo.platform
  resources:
  - llmoptimizedservices/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - apps
  resources:
  - deployments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - keda.sh
  resources:
  - scaledobjects
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - monitoring.coreos.com
  resources:
  - servicemonitors
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: llm-icp-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: llm-icp-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-leader-election-rolebinding
  namespace: llm-icp-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: llm-icp-leader-election-role
subjects:
- kind: ServiceAccount
  name: llm-icp-controller-manager
  namespace: llm-icp-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
  name: llm-icp-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: llm-icp-manager-role
subjects:
- kind: ServiceAccount
  name: llm-icp-controller-manager
  namespace: llm-icp-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: llm-icp-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: llm-icp-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: llm-icp-controller-manager
  namespace: llm-icp-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
    control-plane: controller-manager
  name: llm-icp-controller-manager-metrics-service
  namespace: llm-icp-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: llm-icp
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: llm-icp
    control-plane: controller-manager
  name: llm-icp-controller-manager
  namespace: llm-icp-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: llm-icp
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: llm-icp
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: mlo.platform/local-llm-inference-control-plane:v1
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 8081
          name: health
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: llm-icp-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
